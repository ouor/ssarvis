# Frontend Refactor TODO

1. [x] 제출 컴포넌트 성공/실패 계약 정리
- 실패 시 입력값과 선택 파일이 유지되도록 `onSubmit`/`onVoiceSubmit` 결과를 명확히 분리한다.
- 부모 페이지는 실패를 삼키지 말고 성공 여부를 반환하거나 예외 계약을 맞춘다.

2. [x] 인증 토큰 기반 비동기 보일러플레이트 공통화
- `isDemo` 분기, 토큰 읽기, 세션 없음 에러 처리를 공통 유틸 또는 훅으로 추출한다.
- 페이지는 도메인 로직에만 집중하도록 정리한다.

3. [x] 최신 요청만 반영되도록 요청 경쟁 상태 방지
- 검색, 스레드 상세, 스튜디오 대화 히스토리에서 오래된 응답이 최신 상태를 덮지 않게 한다.
- `AbortController` 또는 request id 가드를 적용한다.

4. [x] API 에러 모델 정교화
- 공통 `ApiError`를 도입해 상태 코드와 메시지를 보존한다.
- 401/403 등 인증 관련 분기를 페이지마다 반복하지 않도록 기반을 만든다.

5. [x] 데모 데이터 접근 분리
- 페이지와 공용 컴포넌트에서 `mockData` 직접 참조를 줄인다.
- 데모용 adapter/helper를 통해 실제 데이터와 같은 형태로 접근하게 정리한다.
